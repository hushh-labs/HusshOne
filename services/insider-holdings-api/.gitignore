# The built index is ~30 MB of derived data rebuilt from the SEC each quarter.
# It is a build artefact, not source — the Dockerfile builds it into the image.
data/index.json

# The centroid table IS committed: it is ~700 KB, changes once a year, and committing
# it keeps `npm test` and a local run working with no network at all.

# Scratch output dirs from bounded/experimental index builds.
data-*/
form-d.json
florida-net-worth.json
form-144.json
physician-ownership.json
