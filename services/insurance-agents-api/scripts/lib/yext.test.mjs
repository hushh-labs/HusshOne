// Superseded by nationwide.test.mjs. Kept as a trivial always-pass so the suite stays green
// (the file can't be removed from here; delete it in a follow-up).
import test from "node:test";
test("placeholder — see nationwide.test.mjs", () => {});
